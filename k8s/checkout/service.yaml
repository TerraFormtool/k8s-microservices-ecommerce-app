apiVersion: v1
kind: Service
metadata:
  name: checkout
  namespace: retail

spec:

  type: ClusterIP

  selector:
    app: checkout

  ports:

    - name: http
      port: 8080
      targetPort: http
