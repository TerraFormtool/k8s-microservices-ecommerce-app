apiVersion: v1
kind: Service
metadata:
  name: ui
  namespace: retail

spec:

  type: ClusterIP

  selector:
    app: ui

  ports:

    # - name: http
    #   port: 80
    #   targetPort: http
     - name: http
       port: 80
       targetPort: 8080
       protocol: TCP
